import * as React from "react"
import Layout from "../components/Layout"

// markup
const NotFoundPage = () => {
  return (
    <Layout>
      Nothing to see here
    </Layout>
  )
}

export default NotFoundPage
